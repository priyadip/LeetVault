# LeetCode Solutions

_Auto-generated by [leetvault](https://github.com/priyadip/LeetVault) - last updated {{ stats.generated_at }}._

## Progress

**{{ stats.total_solved }} problem{{ "s" if stats.total_solved != 1 else "" }} solved**

| Difficulty | Solved | |
| --- | --- | --- |
{% for d in stats.by_difficulty %}
| {{ d.difficulty }} | {{ d.count }} | `{{ d.bar }}` {{ "%.1f"|format(d.percent) }}% |
{% endfor %}

Current streak: **{{ stats.current_streak }}** day{{ "s" if stats.current_streak != 1 else "" }} · Longest streak: **{{ stats.longest_streak }}** day{{ "s" if stats.longest_streak != 1 else "" }}

## Languages

| Language | Solved | |
| --- | --- | --- |
{% for l in stats.by_language %}
| {{ l.lang }} | {{ l.count }} | `{{ l.bar }}` {{ "%.1f"|format(l.percent) }}% |
{% endfor %}

## Topics

{% if stats.by_topic %}
{% for t in stats.by_topic %}`{{ t.name }}` ({{ t.count }}){{ ", " if not loop.last else "" }}{% endfor %}

{% else %}
_No topic data yet._
{% endif %}

## Recently Solved

| Problem | Difficulty | Language | Date |
| --- | --- | --- | --- |
{% for r in stats.recent %}
| [{{ r.title }}]({{ r.url }}) | {{ r.difficulty }} | {{ r.lang }} | {{ r.date }} |
{% endfor %}

## All Solutions

| # | Problem | Difficulty | Language | Date | Solution |
| --- | --- | --- | --- | --- | --- |
{% for row in stats.table %}
| {{ row.frontend_id }} | [{{ row.title }}]({{ row.url }}) | {{ row.difficulty }} | {{ row.lang }} | {{ row.date }} | [{{ row.file_name }}]({{ row.file_path }}) |
{% endfor %}
